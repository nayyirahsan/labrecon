name: Deploy

on:
  # Only deploy after CI passes on main — never deploy a broken build
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      # Install Vercel CLI globally — used for deployment
      - run: npm install -g vercel
      # Deploy to production; Vercel reads env vars from the project dashboard
      - run: vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
